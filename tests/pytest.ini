[pytest]
minversion = 6.0
addopts = -ra -q -l --env=local
testpaths =
    tests
markers =
    integration: mark test as integration to run separately
    env(name): specify environment config to load
